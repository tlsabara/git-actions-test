name: Shell comands

on: push

jobs:
  run-shell-command:
    runs-on: ubuntu-18.04
    steps:
      - name: echo a string
        run: echo "Hello Git Actions World!!"

      - name: multiline script
        run: |
          node -v 
          npm -v
          python -V
